<header class="page-header">
  <h1>Invoices</h1>
  <%= link_to "New invoice", new_invoice_path, class: "btn btn-primary" %>
</header>

<% if @invoices.any? %>
  <table class="data-table">
    <thead>
      <tr>
        <th>Created</th>
        <th>Client</th>
        <th>Due</th>
        <th>Status</th>
        <th class="num">Amount</th>
        <th class="num">Balance due</th>
      </tr>
    </thead>
    <tbody>
      <% @invoices.each do |invoice| %>
        <tr>
          <td><%= invoice.created_at.to_date.iso8601 %></td>
          <td><%= link_to invoice.customer_display, invoice_path(invoice) %></td>
          <td><%= invoice.due_date.iso8601 %></td>
          <td><span class="badge badge-<%= invoice.status %>"><%= invoice.status %></span></td>
          <td class="num">$<%= number_with_precision(invoice.amount, precision: 2, delimiter: ",") %></td>
          <td class="num">$<%= number_with_precision(invoice.balance_due, precision: 2, delimiter: ",") %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p class="empty-state">No invoices yet.</p>
<% end %>
