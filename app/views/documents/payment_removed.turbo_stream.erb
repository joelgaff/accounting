<%= turbo_stream.replace dom_id(@document, :status), partial: "documents/status_header", locals: { document: @document } %>
<%= turbo_stream.replace dom_id(@document, :payments), partial: "documents/payments", locals: { document: @document } %>
